# Dependencies — regenerated by `npm install`
node_modules/

# Expo dev-server cache — machine-specific, regenerated on `npx expo start`
.expo/

# Production / web builds — generated artefacts, not source
dist/
web-build/

# Native build folders (generated by `expo prebuild` / `expo eject`)
android/
ios/

# Environment variables — may contain secrets
.env
.env.local
.env*.local

# OS noise
.DS_Store
Thumbs.db

# Editor noise
.vscode/
*.orig.*
